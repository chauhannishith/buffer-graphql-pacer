# Required for manual live integration scripts (never commit real values).
BUFFER_ACCESS_TOKEN=

# Confirm against current Buffer API documentation before running live tests.
BUFFER_GRAPHQL_URL=

# Live flood scripts (examples/flood-*.ts) require explicit opt-in.
RUN_LIVE_TESTS=0
